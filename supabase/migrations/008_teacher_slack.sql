-- ============================================================
--  008  선생님별 Slack 알림 수신 이메일
--  - 학생 가입 신청 시 담당 선생님에게 개인 DM 을 보내기 위해,
--    선생님의 Slack 이메일(계정 이메일과 다를 수 있음)을 저장.
--  - 비어 있으면 앱에서 로그인 이메일(teachers.email)로 폴백.
-- ============================================================

alter table public.teachers
  add column if not exists slack_email text;
